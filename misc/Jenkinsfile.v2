node{
  stage ('Build') {

    git url: 'https://github.com/Voodoo3000/JMPSpringMVC.git'
    withMaven(maven: 'maven-3') {
      // Run the maven build
      sh "mvn clean verify"

    } // withMaven will discover the generated Maven artifacts, JUnit Surefire & FailSafe & FindBugs & SpotBugs reports...
  }
}
